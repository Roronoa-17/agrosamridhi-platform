server:
  port: 8090

spring:
  application:
    name: dashboard-aggregator-service
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
  cache:
    type: redis
  # OpenFeign Configuration MUST be nested under 'spring:'
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 100
            readTimeout: 300 

# Eureka Discovery Server Configuration
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true